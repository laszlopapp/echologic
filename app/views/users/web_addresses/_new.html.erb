<% content_for :web_address do %>
  <%= select :web_address,
                 :type_id,
                 WebAddressType.all.collect { |wa| [wa.get_current_enum_value.value,wa.id] } %>

      <%= text_field :web_address, :address, :value => '' %>
    
      <%= submit_tag I18n.t('application.general.create'),
                     :class => 'text_button add_text_button',
                     :id => 'new_web_address_submit' %>
<% end %>
<%= render :partial => 'users/components/new', :locals => {:type => 'web_address', :objectClass => WebAddress} %>
