/**
 * Template to change static menu, tabs and static content via JS.
 */

/* Write render output into JS variables for later use. */
var content = "<%= escape_javascript(render(:partial => request[:action])) %>";
var title = "<%= title %>";

$j('#dialogContent').html(content);

$j('#dialogContent').dialog({
  title:    title,
  bgiframe: true,
  modal:    true,
  buttons:  { <%= I18n.t('application.general.close') %>: function() { $j(this).dialog('destroy'); }},
  close:    function(event, ui) { $j(this).dialog('destroy'); },
  width:    750,
  resizable: false,
  dialogClass: "dialog_popup"
});

makeTooltips();

