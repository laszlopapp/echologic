<%
# Render submenu tabs for admin section.

render_tabnav :admin,
              :generate_css => false, :html => {:class => 'submenu'} do

  add_tab do |t|
    t.named "Locales"
    t.links_to hash_for_locales_path
  end

  add_tab do |t|
    t.named "Reports"
    t.links_to hash_for_reports_path
  end

  add_tab do |t|
    t.named "Team"
    t.links_to hash_for_admin_items_path
  end

end
%>
