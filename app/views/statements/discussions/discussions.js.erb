var content = "<%= escape_javascript(render(:partial => 'statements/discussions/discussions', :locals => {:statement_nodes => @statement_nodes})) %>";

$("#discussions_container").replaceWith(content);
