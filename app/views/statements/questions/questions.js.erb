var content = "<%= escape_javascript(render(:partial => 'statements/questions/questions', :locals => {:statements => @statements})) %>";
$("#questions_container").replaceWith(content);
roundCorners();
