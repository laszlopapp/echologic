<% statement_node ||= @statement_node %>
<% statement_document ||= @statement_document %>

<% semantic_form_for statement_node, :html => {:class => "wide_form statement"} do |form| -%>
  <%= render :partial => 'statements/form_header', :locals => {:form => form, :statement_node => statement_node} %>
  <%= render :partial => 'statements/form_content', :locals => {:form => form, :statement_node => statement_node} %>
<% end -%>

<!-- TODO: What to do with these elements?
content_tag :h2, I18n.t("discuss.statements.edit", :type => I18n.t("discuss.statements.types.#{dom_class(statement_node)}"))
content_tag :p, I18n.t('discuss.statements.edit_period_hint', :minutes => 60)
-->
