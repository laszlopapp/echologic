var element = $('#statements div.<%= @statement_node.class.name_for_siblings %>');
var authors = $("<%= escape_javascript(render :partial => 'statements/authors') %>");
element.data('api').loadAuthors(authors);
