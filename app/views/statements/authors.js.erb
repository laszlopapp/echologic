var element = $('#statements div.<%= dom_class(@statement_node) %>');
var authors = $("<%= escape_javascript(render :partial => 'statements/authors') %>");
element.data('api').loadAuthors(authors, <%= @authors.length %>);
