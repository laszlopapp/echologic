<% statement_node ||= @statement_node %>
<% content_tag :div, :class => "header active" do %>
  <%= content_tag :div, prev_next_buttons(statement_node, "add", @type), :class => 'header_buttons'%>
  <% content_tag :div, :class => 'add_header_link'  do %>
	  <% if !statement_node.nil? %>
      <%= create_new_child_statement_link(statement_node,@type,false) %>
		<% else %>
		  <%= create_new_question_link(params[:origin]) %>
		<% end %>
		<%# if statement_node.echoable? %>
      <%#= supporter_ratio_bar(nil, false) %>
		<%# end %>
  <% end %>
<% end %>
