<%
  statement_document = @children_documents[child.statement_id]
  extra_classes ||= (@current_node && child == @current_node ? 'active' : '') 
%>
<% new_level ||= false%>

<% content_tag :li, :class => dom_class(child), 'statement-id' => child.target_id do %>
	<%= link_to_child(statement_document.title, child, :css => extra_classes, :new_level => new_level) %>
  <%= supporter_ratio_bar(child) %>
<% end %>
