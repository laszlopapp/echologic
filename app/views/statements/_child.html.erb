<% statement ||= @statement 
   extra_classes ||= nil %>

<li id="<%= statement_dom_id(statement) %>" class="<%=statement.parent.class.name.downcase%>">
  <%= link_to(statement.title, url_for(statement), :class => "ajax statement_link #{statement.class.name.underscore}_link #{extra_classes} ttLink no_border", :title => I18n.t("discuss.tooltips.read_#{statement.class.name.underscore}")) %>
  <%= supporter_ratio_bar(statement) %>
</li>
