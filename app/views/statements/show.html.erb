<% content_for :submenu, tabnav(:discuss) %>

<%= render :partial => 'statements/context', :locals => {:statement => @statement} %>
<%= render :partial => 'statements/sidebar', :locals => {:statement => @statement}%>
<%= render :partial => 'statements/summary', :locals => {:statement => @statement, :statement_document => @statement_document} %>
<%= render :partial => 'statements/children', :locals => {:statement => @statement, :children => @children } %>

<script type="text/javascript">
  makeRatiobars();
	roundCorners();
  showMessageBox("#translation_box", <%= @translation_permission %>);
	showMessageBox("#original_language_box", <%= @original_language_warning %>);
</script>
