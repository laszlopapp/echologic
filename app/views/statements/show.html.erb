<% content_for :submenu, tabnav(:discuss) %>

<%= render :partial => 'statements/context', :locals => {:statement_node => @statement_node} %>
<%= render :partial => 'statements/sidebar', :locals => {:statement_node => @statement_node}%>
<%= render :partial => 'statements/summary', :locals => {:statement_node => @statement_node,
                                                         :statement_document => @statement_document} %>
<%= render :partial => 'statements/approved', :locals => {:statement_node => @approved_node,
                                                          :statement_document => @approved_document } %>
<%= render :partial => 'statements/children', :locals => {:statement_node => @statement_node,
                                                          :children => @children } %>

<script type="text/javascript">
  makeRatiobars();
	roundCorners();
  showMessageBox("#translation_box", <%= @translation_permission %>);
	showMessageBox("#original_language_box", <%= @original_language_warning %>);
</script>
