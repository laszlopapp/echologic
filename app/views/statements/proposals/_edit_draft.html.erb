<% statement_node ||= @statement_node -%>
<% tags ||= @tags %>

<% semantic_form_for statement_node, :html => {:class => "incorporate wide_form statement"} do |form| -%>
  <%= render :partial => 'statements/form_header', :locals => {:form => form, :statement_node => statement_node} %>
	<%= render :partial => 'statements/form_content', :locals => {:form_partial => 'statements/proposals/incorporation_form', 
                                                                :form => form, 
                                                                :statement_node => statement_node} %>
<% end -%>
