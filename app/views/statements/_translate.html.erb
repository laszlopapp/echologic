<% statement ||= @statement -%>

<div id="statement_translate" class="header_block discuss_right_block">
  <h2><%= I18n.t('discuss.translation_request') %></h2>
  <p><%= I18n.t('discuss.translation_request') %></p>
  <%= render :partial => 'statements/translation_form', :locals => {:statement => statement} %>
</div>

<% unless statement.class.name == 'Question' %>
  <script type="text/javascript">
    $('#new_<%=statement_class_dom_id(statement)%>').ajaxForm({ dataType : 'script' });
  </script>
<% end %>
