<% statement_node ||= @statement_node -%>
<% old_statement_document ||= @statement_document%>
<% new_statement_document ||= @new_statement_document %>



<% semantic_form_for statement_node, :url => create_translation_url(statement_node, dom_class(statement_node)),
                                     :html => {:class => "ajax_form wide_form translation statement"} do |form| -%>
  <%= render :partial => 'statements/header', :locals => {:statement_node => statement_node} %>
  <%= render :partial => 'statements/form_content', :locals => {:form_partial => 'statements/translation_form', 
                                                                :form => form, 
                                                                :statement_node => statement_node} %>
<% end -%>

