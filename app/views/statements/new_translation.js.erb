/* Close the content of the remaining ones */
collapseStatements();

var new_form = "<%= escape_javascript(render :partial => 'statements/translate') %>";
replaceOrInsert($('#statements div.<%= dom_class(@statement_node) %>'), new_form);

roundCorners();
$('#edit_<%= dom_id(@statement_node)%>').ajaxForm({ dataType : 'script' });