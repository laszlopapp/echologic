<% content_for :submenu, tabnav(:discuss) %>
<%# TODO locals? %>
<%= render :partial => 'statements/new', :locals => {:statement => @statement, :tags => @tags, :statement_document => @statement_document} %>

