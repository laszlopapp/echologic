<% %w(header action_bar).each do |element| %>
  var element = "<%= escape_javascript(render :partial => "statements/#{element}") %>";
  $('#statements div.<%= dom_class(@statement_node) %> .<%= element %>').replaceWith(element);
<% end %>

makeRatiobars();
makeTooltips();
