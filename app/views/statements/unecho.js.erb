<% if defined? @info %>
  info('<%=@info%>');
<% end %>
<% if defined? @error %>
  error('<%=@error%>');
<% end %>

var element = $('#statements div#<%= dom_id(@statement_node)%>');

<% href = @statement_node.supported?(current_user) ? unecho_statement_node_url(@statement_node) : echo_statement_node_url(@statement_node) %>
var new_supporters_bar = "<%= escape_javascript(supporter_ratio_bar(@statement_node)) %>";
var new_supporters_number = "<%= escape_javascript(supporters_number(@statement_node)) %>";

element.data('echoableApi').updateState('<%= href %>', new_supporters_bar, new_supporters_number);

//element.data('api').reinitialise();