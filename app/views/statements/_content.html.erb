<% statement_node ||= @statement_node %>
<% hide_content ||= false %>

<% content_tag(:div, :class => 'expandable_content content', :style => (hide_content ? 'display: none;' : '')) do %>
	 <%= render_alternatives(statement_node, @children[:Alternative]) if statement_node.class.has_alternatives? and !alternative_mode?(statement_node) %>
	 <%= render :partial => 'statements/action_bar',
              :locals => {:statement_node => statement_node}%>
	 <%= render :partial => 'statements/summary',
              :locals => {:statement_node => statement_node,
                          :statement_document => @statement_document,
                          :approved_node => @approved_node,
                          :approved_document => @approved_document} %>
	 <%= render_all_children(statement_node, @children) %>
	 <%= render_embedded_content(statement_node) if statement_node.class.has_embeddable_data? %>

<% end %>
